---
import AdminLayout from '@layouts/AdminLayout.astro'
import {
    getTitleTag,
    getMetaTag,
    getBodyTag,
    getLoaderTag,
} from '@wf/lib.worker'
---

<AdminLayout 
    reload={''}
    loader={getLoaderTag()}
    title={getTitleTag()} 
    meta={getMetaTag()} 
    body={getBodyTag()}>
</AdminLayout>